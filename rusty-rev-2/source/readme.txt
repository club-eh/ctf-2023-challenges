1. Put the flag in `flag.txt`
2. Run `./build.sh` to build the executable, placed in out/ (and ../static/)

Requires:
- `cargo` (1.65)
- `cbindgen`
- `gcc` (12.2)
- `python`
- `bat` (can be manually removed from build.sh)
